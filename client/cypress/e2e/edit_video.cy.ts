describe('edit video', () => {
  before(() => cy.setRootPath());
  beforeEach(() => cy.visit(Cypress.env('root_url')));

  it.skip('updates title when changing title', () => {
  });

  it.skip('updates playlist when changing playlist', () => {
  });

  it.skip('shows delete warning when delete clicked', () => {
  });

  it.skip('closes delete warning when cancel clicked', () => {
  });

  it.skip('closes modal and deletes video when delete clicked', () => {
  });
});